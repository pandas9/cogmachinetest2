# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - "torch==2.0.1"
    - "transformers==4.43.3"
    - "torchvision==0.15.2"
    - "einops==0.7.0"
    - "safetensors==0.4.1"
    - "pillow==9.4.0"

predict: "predict.py:Predictor"
