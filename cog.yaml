# Configuration for Cog ⚙️
# Reference: https://cog.run/yaml

build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - "torch==2.5.0"
    - "transformers==4.45.2"
    - "torchvision==0.20.0"
    - "einops==0.7.0"
    - "safetensors==0.4.5"
    - "pillow==9.5.0"

predict: "predict.py:Predictor"
